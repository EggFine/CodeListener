plugins {
    id 'java'
}

group = 'com.blbilink.codeListener'
version = '1.0'

jar {
    manifest {
        attributes(
                'Main-Class': 'com.blbilink.codeListener.Main'
        )
    }
}

repositories {
    mavenCentral()
}

dependencies {
    testImplementation platform('org.junit:junit-bom:5.10.0')
    testImplementation 'org.junit.jupiter:junit-jupiter'
}

test {
    useJUnitPlatform()
}